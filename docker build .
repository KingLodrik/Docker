mkdir <directory nmae>
cd <direcorty>
vi <file name>

example:-
#!/bin/sh

from alpine

Run apk add --update redis

CMD ["redis-server"]


come badck to directory
docker build . (build the custome image)

**when its craete docker image get the number and run
                      docker run <image ID>
**after thate start get docker ID
                        docker start <contariner ID>
                        
                       
